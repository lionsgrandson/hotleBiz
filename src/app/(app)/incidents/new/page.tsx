import { requireHotelContext, WRITE_ROLES } from '@/lib/auth'
import { guestDisplay } from '@/lib/data'
export default async function NewIncident({searchParams}:{searchParams:Promise<{guest?:string;stay?:string}>}){const p=await searchParams;const{admin,hotel}=await requireHotelContext(WRITE_ROLES);const{data:links}=await admin.from('guest_hotel_links').select('guest_id,guest:guests(*)').eq('hotel_id',hotel.id).limit(250);const guests=(links||[]).map((x:any)=>guestDisplay(x.guest));return <><div className="topline"><div><span className="eyebrow">Evidence-first workflow</span><h1>Document an incident</h1></div></div><form className="card simpleForm" action="/api/incidents" method="post" encType="multipart/form-data"><div className="notice">Severity 3–4 records are not published to the network immediately. They enter a manager review queue first. Describe observable facts, not diagnoses, protected traits, rumors, or assumptions about motive.</div><label>Guest<select name="guestId" defaultValue={p.guest||''} required><option value="" disabled>Select guest</option>{guests.map(g=><option key={g.id} value={g.id}>{g.legalName} · {g.countryCode||'—'}</option>)}</select></label>{p.stay&&<input type="hidden" name="stayId" value={p.stay}/>}<label>Category<select name="category"><option value="property_damage">Property damage</option><option value="threats_violence">Threats / violence</option><option value="theft_report">Reported theft</option><option value="harassment">Harassment</option><option value="noise_disturbance">Noise / disturbance</option><option value="smoking">Smoking violation</option><option value="unauthorized_guests">Unauthorized guests</option><option value="payment_dispute">Payment dispute</option><option value="fraud_suspicion">Suspected fraud</option><option value="security_intervention">Security / police intervention</option><option value="other">Other</option></select></label><label>Severity<select name="severity"><option value="1">1 · Minor</option><option value="2">2 · Material</option><option value="3">3 · Serious · review required</option><option value="4">4 · Critical · review required</option></select></label><label>Evidence level<select name="evidenceLevel"><option value="observed">Directly observed by staff</option><option value="documented">Documented / corroborated</option><option value="reported">Reported, not independently verified</option></select></label><label>Occurred at<input name="occurredAt" type="datetime-local" required /></label><label>Title<input name="title" maxLength={180} required /></label><label>Factual description<textarea name="description" maxLength={5000} required /></label><label>External reference<input name="externalRef" placeholder="Security log / police / payment reference, if applicable" /></label><label>Evidence file<input name="evidence" type="file" accept="image/jpeg,image/png,image/webp,application/pdf,text/plain" /></label><label><span><input name="attest" type="checkbox" required /> I confirm this record is relevant to the stay and written as factual observations or clearly identified reports.</span></label><button className="primary">Submit incident</button></form></>}
