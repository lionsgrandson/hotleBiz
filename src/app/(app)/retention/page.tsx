import { requireHotelContext, MANAGE_ROLES } from '@/lib/auth'
export default async function Retention(){const{admin,hotel}=await requireHotelContext(MANAGE_ROLES);const{data}=await admin.from('retention_queue').select('*').eq('hotel_id',hotel.id).order('eligible_at',{ascending:true}).limit(250);return <><div className="topline"><div><span className="eyebrow">Data lifecycle</span><h1>Retention review</h1></div><form action="/api/retention" method="post"><button className="secondary" name="decision" value="refresh">Refresh candidates</button></form></div><section className="card panel"><div className="notice">Items are queued when they exceed the property’s configured retention period. Approval does not automatically delete evidence or records: a deletion/pseudonymization policy must be approved by counsel before destructive processing is enabled.</div><table className="table"><thead><tr><th>Eligible</th><th>Record</th><th>Reason</th><th>Status</th><th>Review</th></tr></thead><tbody>{(data||[]).map((x:any)=><tr key={x.id}><td>{new Date(x.eligible_at).toLocaleDateString()}</td><td>{x.target_type} · {String(x.target_id).slice(0,10)}</td><td>{x.reason}</td><td>{x.status}</td><td><form action="/api/retention" method="post" className="actions"><input type="hidden" name="id" value={x.id}/><button className="secondary" name="decision" value="approved">Approve</button><button className="secondary" name="decision" value="deferred">Defer</button></form></td></tr>)}</tbody></table></section></>}
