import { requireHotelContext, MANAGE_ROLES } from '@/lib/auth'
export default async function Team({searchParams}:{searchParams:Promise<{invite?:string}>}){const sp=await searchParams;const{admin,hotel}=await requireHotelContext(MANAGE_ROLES);const{data}=await admin.from('hotel_memberships').select('id,user_id,role,status,created_at,profile:profiles(full_name,email)').eq('hotel_id',hotel.id).order('created_at');return <><div className="topline"><div><span className="eyebrow">Least privilege</span><h1>Property team</h1></div></div>{sp.invite&&<section className="card simpleForm"><span className="eyebrow">Invitation created</span><label>Shareable invite URL<input readOnly value={sp.invite}/></label></section>}<section className="dashboardGrid"><div className="card panel"><h2>Members</h2><table className="table"><thead><tr><th>Name</th><th>Email</th><th>Access</th><th>Manage</th></tr></thead><tbody>{(data||[]).map((m:any)=><tr key={m.id}><td>{m.profile?.full_name||'—'}</td><td>{m.profile?.email||'—'}</td><td>{m.role} · {m.status}</td><td>{m.role==='owner'?'Owner':<form action="/api/team/member" method="post" className="actions"><input type="hidden" name="memberId" value={m.id}/><select name="role" defaultValue={m.role}><option value="viewer">Viewer</option><option value="reviewer">Reviewer</option><option value="manager">Manager</option><option value="admin">Admin</option></select><select name="status" defaultValue={m.status}><option value="active">Active</option><option value="suspended">Suspended</option><option value="revoked">Revoked</option></select><button className="secondary">Save</button></form>}</td></tr>)}</tbody></table></div><form className="card simpleForm" action="/api/team/invite" method="post"><span className="eyebrow">Invite staff</span><label>Email<input name="email" type="email" required/></label><label>Role<select name="role"><option value="viewer">Viewer · search/read only</option><option value="reviewer">Reviewer · add stays/feedback/incidents</option><option value="manager">Manager · review serious incidents/disputes</option><option value="admin">Admin · manage staff</option></select></label><button className="primary">Create invitation</button></form></section></>}
