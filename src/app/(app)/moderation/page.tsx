import { requireHotelContext, MANAGE_ROLES } from '@/lib/auth'
import { decryptPII } from '@/lib/crypto'
export default async function Moderation(){const{admin,hotel}=await requireHotelContext(MANAGE_ROLES);const{data}=await admin.from('incidents').select('id,guest_id,title_cipher,category,severity,description_cipher,evidence_level,occurred_at,created_at').eq('hotel_id',hotel.id).eq('status','pending_review').order('created_at',{ascending:true});return <><div className="topline"><div><span className="eyebrow">Two-person control</span><h1>Review queue</h1></div></div>{!data?.length?<section className="card empty">No records require review.</section>:data.map((i:any)=><section className="card panel section" key={i.id}><span className="eyebrow">Severity {i.severity}/4 · {i.evidence_level}</span><h2>{decryptPII(i.title_cipher)}</h2><p>{decryptPII(i.description_cipher)}</p><p style={{color:'#66716c'}}>{i.category.replaceAll('_',' ')} · {new Date(i.occurred_at).toLocaleString()}</p><form action="/api/moderation" method="post" className="actions"><input type="hidden" name="incidentId" value={i.id}/><button className="primary" name="decision" value="publish">Approve & publish</button><button className="dangerBtn" name="decision" value="reject">Reject / remove</button></form></section>)}</>}
