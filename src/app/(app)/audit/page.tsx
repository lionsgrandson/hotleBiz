import { requireHotelContext, MANAGE_ROLES } from '@/lib/auth'
export default async function Audit(){const{admin,hotel}=await requireHotelContext(MANAGE_ROLES);const{data}=await admin.from('audit_logs').select('id,user_id,action,target_type,target_id,purpose,metadata,created_at').eq('hotel_id',hotel.id).order('created_at',{ascending:false}).limit(250);return <><div className="topline"><div><span className="eyebrow">Append-only accountability</span><h1>Audit log</h1></div></div><section className="card panel"><table className="table"><thead><tr><th>Time</th><th>Action</th><th>User</th><th>Target</th><th>Purpose</th></tr></thead><tbody>{(data||[]).map((x:any)=><tr key={x.id}><td>{new Date(x.created_at).toLocaleString()}</td><td>{x.action.replaceAll('_',' ')}</td><td><code>{String(x.user_id||'system').slice(0,8)}</code></td><td>{x.target_type||'—'} {x.target_id?String(x.target_id).slice(0,10):''}</td><td>{x.purpose||'—'}</td></tr>)}</tbody></table></section></>}
