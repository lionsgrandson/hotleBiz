import { requireHotelContext, MANAGE_ROLES } from '@/lib/auth'
import { decryptPII } from '@/lib/crypto'
export default async function Disputes(){const{admin,hotel}=await requireHotelContext(MANAGE_ROLES);const{data}=await admin.from('disputes').select('*').eq('source_hotel_id',hotel.id).order('created_at',{ascending:false}).limit(100);return <><div className="topline"><div><span className="eyebrow">Correction & challenge rights</span><h1>Disputes</h1></div></div><section className="card panel">{!data?.length?<p className="empty">No disputes received.</p>:<table className="table"><thead><tr><th>Date</th><th>Type</th><th>Request</th><th>Status</th><th>Resolution</th></tr></thead><tbody>{data.map((d:any)=><tr key={d.id}><td>{new Date(d.created_at).toLocaleDateString()}</td><td>{d.request_type}</td><td>{decryptPII(d.message_cipher)||'—'}</td><td>{d.status}</td><td>{['pending','reviewing'].includes(d.status)?<form action="/api/disputes" method="post" style={{display:'grid',gap:6}}><input type="hidden" name="disputeId" value={d.id}/><textarea name="response" placeholder="Explain the decision to the guest" required/><textarea name="replacementText" placeholder="Optional corrected text. Leave blank to withdraw the record when accepting."/><div className="actions"><button className="primary" name="decision" value="accepted">Accept / correct</button><button className="secondary" name="decision" value="rejected">Reject / keep record</button></div></form>:decryptPII(d.response_cipher)||'Closed'}</td></tr>)}</tbody></table>}</section></>}
