import { requireUser } from '@/lib/auth'
import { createHotel } from './actions'

export default async function Onboarding() {
  await requireUser()
  return <main className="legal"><span className="eyebrow">Property onboarding</span><h1>Create your hotel workspace</h1><p>GuestAtlas is a shared personal-data system. Before a property contributes data, an authorized manager must accept the network rules and confirm the property has a lawful basis for collecting and using the information it enters.</p><form className="card simpleForm"><label>Hotel / property name<input name="name" required /></label><label>Legal entity name<input name="legalEntityName" required /></label><label>Company / registration number<input name="registrationNumber" required /></label><label>City<input name="city" required /></label><label>Country code<input name="countryCode" defaultValue="IL" maxLength={2} required /></label><label>Legal / privacy contact email<input name="legalEmail" type="email" required /></label><label><span><input name="legalBasis" type="checkbox" required /> I confirm the property will record factual stay information only, will not enter protected traits or irrelevant sensitive information, and will handle correction/dispute requests.</span></label><div className="notice">New properties start in verification review. Team setup remains available while verification is pending. Guest personal-data processing and cross-hotel lookup stay locked until GuestAtlas verifies the property.</div><button className="primary" formAction={createHotel}>Submit property</button></form></main>
}
