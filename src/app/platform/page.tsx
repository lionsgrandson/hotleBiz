import { redirect } from 'next/navigation'
import { requireUser, isPlatformAdmin, requireMfa } from '@/lib/auth'
import { createAdminClient } from '@/lib/supabase/admin'
export const dynamic='force-dynamic'
export default async function Platform(){await requireMfa();const user=await requireUser();if(!await isPlatformAdmin(user.id))redirect('/dashboard?error=permission');const admin=createAdminClient();const[{data:hotels},{count:guests},{count:searches},{count:disputes}]=await Promise.all([admin.from('hotels').select('id,name,legal_entity_name,registration_number,city,country_code,legal_contact_email,verification_status,created_at').order('created_at',{ascending:false}),admin.from('guests').select('*',{count:'exact',head:true}),admin.from('audit_logs').select('*',{count:'exact',head:true}).eq('action','guest_network_search_requested'),admin.from('disputes').select('*',{count:'exact',head:true}).in('status',['pending','reviewing'])]);return <main className="legal" style={{maxWidth:1200}}><span className="eyebrow">Network governance</span><h1>Platform administration</h1><section className="metricGrid"><div className="card metric"><span>Properties</span><strong>{hotels?.length||0}</strong></div><div className="card metric"><span>Guest records</span><strong>{guests||0}</strong></div><div className="card metric"><span>Audited searches</span><strong>{searches||0}</strong></div><div className="card metric"><span>Open disputes</span><strong>{disputes||0}</strong></div></section><section className="card panel section"><h2>Property verification</h2><table className="table"><thead><tr><th>Property</th><th>Legal entity</th><th>Registration</th><th>Contact</th><th>Status</th><th>Decision</th></tr></thead><tbody>{(hotels||[]).map((h:any)=><tr key={h.id}><td><strong>{h.name}</strong><br/>{h.city}, {h.country_code}</td><td>{h.legal_entity_name||'—'}</td><td>{h.registration_number||'—'}</td><td>{h.legal_contact_email}</td><td>{h.verification_status}</td><td><form action="/api/platform/hotels" method="post" className="actions"><input type="hidden" name="hotelId" value={h.id}/><button className="primary" name="decision" value="verified">Verify</button><button className="dangerBtn" name="decision" value="rejected">Reject</button></form></td></tr>)}</tbody></table></section><p><a href="/dashboard">← Back to property</a></p></main>}
